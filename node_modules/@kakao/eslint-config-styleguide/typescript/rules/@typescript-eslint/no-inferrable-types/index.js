const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = '숫자, 문자열, 불리언으로 초기화된 변수 또는 매개변수의 타입을 불필요하게 명시하지 않는다.';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.TYPES;

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
};

// for import plugin check
import path from 'path';

// for babel-parse check
const test = async () => 1;

const check = () => {
  const obj = {
    a: 'a',
    b: 'b',
  };

  obj.c = 'c';

  obj.d = 3000;

  return obj;
};

console.log(check(), test(), path.resolve);

const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = 'NaN 과 비교할 때는 isNaN() 을 사용한다.';

const rule = 'use-isnan';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.POSSIBLE_ERRORS;

const airbnbCategory = AIRBNB_CATEGORY.COMPARISON_OPERATORS_N_EQUALITY;

/* eslint-disable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, eqeqeq */
/* eslint-disable use-isnan */
const bad = () => {
  if (foo == NaN) {
    // ...
  }

  if (foo != NaN) {
    // ...
  }

  if (foo === NaN) {
    // ...
  }

  if (foo !== NaN) {
    // ...
  }
};
/* eslint-enable use-isnan */

const good = () => {
  if (isNaN(foo)) {
    // ...
  }

  if (!isNaN(foo)) {
    // ...
  }
};

const best = null;
/* eslint-enable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, eqeqeq */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best
};

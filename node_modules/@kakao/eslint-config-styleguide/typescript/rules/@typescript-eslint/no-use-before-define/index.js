const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = '변수가 정의되기 전에 사용하지 않는다.';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.VARIABLES;

const airbnbCategory = AIRBNB_CATEGORY.VARIABLES;

const disableRule = {
  name: 'no-use-before-define',
  option: 'off',
};

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  disableRule
};

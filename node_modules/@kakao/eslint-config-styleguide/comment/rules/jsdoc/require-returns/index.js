const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = '함수에 반환값이 있다면 JSDoc 주석에는 returns 항목이 있어야 한다';

const desc = null;

const options = 'error';
// const options = ['error', {forceReturnsWithAsync: true}];

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.COMMENTS;

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
};

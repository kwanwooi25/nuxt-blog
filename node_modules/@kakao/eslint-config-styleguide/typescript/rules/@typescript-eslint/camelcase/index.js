const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = '변수나 함수, 프로퍼티 이름 사용 시 카멜케이스(camelcase)를 사용한다.';

const desc = null;

const options = ['error', { "properties": "always" }];

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.NAMING_CONVENTIONS;

const disableRule = {
  name: 'camelcase',
  option: 'off',
};

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  disableRule,
};

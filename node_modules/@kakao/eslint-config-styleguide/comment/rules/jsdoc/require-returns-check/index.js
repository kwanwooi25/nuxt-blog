const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = 'JSDoc 주석의 returns 항목은 한 개만 있어야 하며, 올바르게 작성되어야 한다.';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.COMMENTS;

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
};


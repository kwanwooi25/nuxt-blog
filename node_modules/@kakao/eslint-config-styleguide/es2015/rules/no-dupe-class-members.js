const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '클래스 멤버끼리 중복된 이름을 사용하지 않는다.';

const rule = 'no-dupe-class-members';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.POSSIBLE_ERRORS;

const airbnbCategory = AIRBNB_CATEGORY.CLASSES_N_CONSTRUCTORS;

/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */
/* eslint-disable no-dupe-class-members */
const bad = () => {
  class Foo1 {
    bar () { }
    bar () { }
  }

  class Foo2 {
    bar () { }
    get bar () { }
  }

  class Foo3 {
    static bar () { }
    static bar () { }
  }
};
/* eslint-enable no-dupe-class-members */

const good = () => {
  class Foo1 {
    bar () { }
    qux () { }
  }

  class Foo2 {
    get bar () { }
    set bar (value) { }
  }

  class Foo3 {
    static bar () { }
    bar () { }
  }
};

const best = null;
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

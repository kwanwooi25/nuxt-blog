const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = '후위 연산자 \'!\'를 사용한  non-null-assertions 을 허용하지 않는다.';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.TYPES;

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
};

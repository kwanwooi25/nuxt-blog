const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '암시적 반환을 하는 화살표 함수 몸체의 위치를 화살표 함수에 표시한다.';

const rule = 'implicit-arrow-linebreak';

const desc = '함수 선언과 몸체 사이에 개행이 있을 때는 함수 선언에 몸체를 표시한다';

const options = ['error', 'beside'];

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.ARROW_FUNCTIONS;

/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */
/* eslint-disable implicit-arrow-linebreak */
const bad = () => {
  foo =>
    bar;

  foo =>
    (bar);

  foo =>
    bar =>
      baz;

  foo =>
    (
      bar()
    );
};
/* eslint-enable implicit-arrow-linebreak */

const good = () => {
  foo => bar;

  foo => (bar);

  foo => bar => baz;

  foo => (
    bar()
  );

  foo => bar();

  foo => bar();
};

const best = null;
/* eslint-enable no-unused-vars */
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

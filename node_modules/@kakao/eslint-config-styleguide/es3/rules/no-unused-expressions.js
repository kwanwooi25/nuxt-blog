const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '의미 없는 표현식을 사용하지 않는다.';

const rule = 'no-unused-expressions';

const desc = null;

const options = [
  'error',
  {
    allowShortCircuit: true,
    allowTernary: true,
    allowTaggedTemplates: true
  }
];

const cause = null;

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.ETC;

/* eslint-disable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, no-constant-condition, no-lone-blocks */
/* eslint-disable no-unused-expressions */
const bad = () => {
  0;

  if (0) 0;

  { 0; }

  a() && function anIncompleteIIFE () {};

  a && b;

  foo || 0;

  a ? b : 0;

  a ? b : c();
};
/* eslint-enable no-unused-expressions */

const good = () => {
  a && b();

  var a = foo || 0;

  a ? b() : c();
};

const best = null;
/* eslint-enable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, no-constant-condition, no-lone-blocks */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best
};

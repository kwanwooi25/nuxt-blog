const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = 'Symbol 생성자를 사용하지 않는다.';

const rule = 'no-new-symbol';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.POSSIBLE_ERRORS;

const airbnbCategory = AIRBNB_CATEGORY.ETC;

/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */
/* eslint-disable no-new-symbol */
const bad = () => {
  const foo = new Symbol('foo');
};
/* eslint-enable no-new-symbol */

const good = () => {
  const foo = Symbol('foo');

  function bar (Symbol) {
    const baz = new Symbol('baz');
  }
};

const best = null;
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '동일 이름으로 재할당하지 않는다.';

const rule = 'no-useless-rename';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.MODULES;

const bad = `
import {foo as foo} from 'bar';
export {foo as foo};
let {foo: foo} = bar;
`;

const good = `
import {foo} from 'bar';
export {foo};
let {foo} = bar;
`;

const best = null;

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

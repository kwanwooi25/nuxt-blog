const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = 'interface, type 선언시 멤버 구분자로 세미콜론(;)을 사용한다.';

const desc = '마지막 멤버에도 구분자를 추가해야 하며, 한 줄 선언인 경우 구분자를 생략한다.';

const options = ['error', {
  "singleline": {
    "delimiter": "semi",
    "requireLast": false,
  }
}];

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.SEMICOLONS;

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
};

const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = 'generator 사용 시 function 과 별표(*) 사이를 붙여 쓰고, 뒤에는 공백을 추가한다.';

const rule = 'generator-star-spacing';

const desc = null;

const options = ['error', {before: false,
  after: true}];

const cause = '별표(\\*)가 연산자로 사용된다기보다는 function\\* 자체가 하나의 의미를 가지기 때문이다.';

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.ITERATORS_N_GENERATORS;

/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */
/* eslint-disable generator-star-spacing */
const bad = () => {
  function * generator () {}
  function *generator () {}
  function*generator () {}

  const func1 = function * () {};
  const func2 = function *() {};
  const func3 = function*() {};

  const func4 = {*generator () {}};
};
/* eslint-enable generator-star-spacing */

const good = () => {
  function* generator () {}

  const func1 = function* () {};

  const func2 = {* generator () {}};
};

const best = null;
/* eslint-enable no-unused-vars */
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

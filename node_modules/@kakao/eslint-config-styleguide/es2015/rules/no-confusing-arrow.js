const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '화살표 함수 구문과 비교 연산자를 혼동되지 않도록 사용한다.';

const rule = 'no-confusing-arrow';

const desc = '괄호를 통해서 함수 구문과 연산자를 구분 짓는다.';

const options = ['error', {allowParens: true}];

const cause = null;

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.ARROW_FUNCTIONS;

/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare, no-magic-numbers */
/* eslint-disable no-confusing-arrow */
const bad = () => {
  const foo = a => bar ? 2 : 3;
};
/* eslint-enable no-confusing-arrow */

const good = () => {
  const foo = a => (bar ? 2 : 3);
};

const best = null;
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare, no-magic-numbers */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

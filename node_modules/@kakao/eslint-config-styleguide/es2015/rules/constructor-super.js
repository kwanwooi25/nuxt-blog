const {
  LINT_CATEGORY,
  AIRBNB_CATEGORY,
} = require('../../lib/generator/constants');

const title = '파생 클래스의 생성자에서 super()을 호출한다.';

const rule = 'constructor-super';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.POSSIBLE_ERRORS;

const airbnbCategory = AIRBNB_CATEGORY.CLASSES_N_CONSTRUCTORS;

/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */
/* eslint-disable constructor-super */
const bad = () => {
  class A {}

  class B extends A {
    constructor () {}
  }

  class C extends null {
    constructor () {
      super();
    }
  }

  class D extends null {
    constructor () {}
  }
};
/* eslint-enable constructor-super */

const good = () => {
  class A {
    constructor () {}
  }

  class B extends A {
    constructor () {
      super();
    }
  }
};

const best = null;

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

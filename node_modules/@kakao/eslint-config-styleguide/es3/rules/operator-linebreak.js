const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '여러 줄의 연산자를 사용할 경우 줄의 끝에 연산자를 쓴다.';

const rule = 'operator-linebreak';

const desc = '단, 삼항연산자는 줄의 시작에 연산자를 둔다.';

const options = [
  'error',
  'after',
  {
    overrides: {
      '?': 'before',
      ':': 'before'
    }
  }
];

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.VARIABLES;

/* eslint-disable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable */
/* eslint-disable operator-linebreak */
const bad = () => {
  foo = 1
    +
    2;

  foo = 1
    + 2;

  foo
    = 5;

  if (someCondition
    || otherCondition) {
  }

  answer = everything ?
    42 :
    foo;
};
/* eslint-enable operator-linebreak */

const good = () => {
  foo = 1 + 2;

  foo = 1 +
    2;

  foo =
    5;

  if (someCondition ||
    otherCondition) {
  }

  answer = everything
    ? 42
    : foo;
};

const best = null;
/* eslint-enable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best
};

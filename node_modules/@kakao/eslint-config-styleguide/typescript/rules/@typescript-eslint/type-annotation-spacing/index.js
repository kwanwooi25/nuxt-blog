const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = 'type annotation 뒤에 공백을 추가한다.';

const desc = '콜론(:)의 경우 뒤에만 공백을 추가하고, 화살표 함수의 경우 화살표 앞뒤에 공백을 추가한다.';

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.WHITESPACE;

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
};

const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '불필요한 boolean 캐스트를 피한다.';

const rule = 'no-extra-boolean-cast';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.POSSIBLE_ERRORS;

const airbnbCategory = AIRBNB_CATEGORY.TYPE_CASTING_N_COERCION;

/* eslint-disable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, no-unmodified-loop-condition */
/* eslint-disable no-extra-boolean-cast */
const bad = () => {
  var foo = !!!bar;

  var foo = !!bar ? baz : bat;

  var foo = Boolean(!!bar);

  if (!!foo) {
    // ...
  }

  if (Boolean(foo)) {
    // ...
  }

  while (!!foo) {
    // ...
  }

  do {
    // ...
  } while (Boolean(foo));

  for (; !!foo;) {
    // ...
  }
};
/* eslint-enable no-extra-boolean-cast */

const good = () => {
  var foo = !!bar;
  var foo = Boolean(bar);

  function foo () {
    return !!bar;
  }

  var foo = bar ? !!baz : !!bat;
};

const best = null;
/* eslint-enable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, no-unmodified-loop-condition */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best
};

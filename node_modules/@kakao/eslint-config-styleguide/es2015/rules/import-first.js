const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '모든 import 구문을 파일의 최상위에 위치시킨다.';

const rule = 'import/first';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.MODULES;

const bad = `
import foo from 'foo';
foo.init();

import bar from 'bar';
`;

const good = `
import foo from 'foo';
import bar from 'bar';

foo.init();
`;

const best = null;

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

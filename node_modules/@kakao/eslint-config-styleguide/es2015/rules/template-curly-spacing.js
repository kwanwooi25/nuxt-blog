const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '템플릿 문자열 사용 시 중괄호 내부에 공백을 제거한다.';

const rule = 'template-curly-spacing';

const desc = null;

const options = ['error', 'never'];

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.STRINGS;


/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */
/* eslint-disable template-curly-spacing */
const bad = () => {
  `hello, ${ people.name}!`;
  `hello, ${people.name }!`;
  `hello, ${ people.name }!`;
};
/* eslint-enable template-curly-spacing */

const good = () => {
  `hello, ${people.name}!`;

  `hello, ${
    people.name
  }!`;
};

const best = null;
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

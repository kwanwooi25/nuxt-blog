const {build} = require('./generator');
const {RULE_DEF_TYPE} = require('./constants');

const [, , targetDir] = process.argv;

build({
  targetDir,
  ruleDefType: RULE_DEF_TYPE.SEPARATED_BY_FOLDER,
});

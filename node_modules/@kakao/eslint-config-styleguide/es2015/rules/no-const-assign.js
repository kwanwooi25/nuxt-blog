const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = 'const에 재할당하지 않는다.';

const rule = 'no-const-assign';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.VARIABLES;

const airbnbCategory = AIRBNB_CATEGORY.VARIABLES;

/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare, no-magic-numbers */
/* eslint-disable no-const-assign */
const bad = () => {
  const a = 0;
  a = 1;

  const b = 0;
  b += 1;
};
/* eslint-disable no-const-assign */

const good = () => {
  const a = 0;
  console.log(a);

  for (const a in [1, 2, 3]) {
    console.log(a);
  }
};

const best = null;
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare, no-magic-numbers */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

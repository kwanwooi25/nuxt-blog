const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = 'Type assertion 사용시, <Type> 대신 as Type 을 사용한다.';

const desc = null;

const options = [
  'error',
  {
    assertionStyle: 'as',
    objectLiteralTypeAssertions: 'allow',
  },
];

const cause = 'JSX 문법과 유사하여 파싱에 문제를 일으킬 수 있다.';

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.TYPES;

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
};

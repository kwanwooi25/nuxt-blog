const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '문자열을 결합하는 경우 템플릿 문자열을 사용한다.';

const rule = 'prefer-template';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.STRINGS;


/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare, no-magic-numbers */
/* eslint-disable prefer-template */
const bad = () => {
  const str1 = 'Hello, ' + name + '!';
  const str2 = 'Time: ' + (12 * 60 * 60 * 1000);
};
/* eslint-disable prefer-template */

const good = () => {
  const str1 = 'Hello World!';
  const str2 = `Hello, ${name}!`;
  const str3 = `Time: ${12 * 60 * 60 * 1000}`;

  const str4 = 'Hello, ' + 'World!';
};

const best = null;
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare, no-magic-numbers */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = 'yield에서 별표(*) 사용 시 별표 뒤에만 공백을 추가한다.';

const rule = 'yield-star-spacing';

const desc = null;

const options = ['error', {before: false,
  after: true}];

const cause = '별표(\\*)가 연산자로 사용된다기보다는 yield\\* 자체가 하나의 의미를 가지기 때문이다.';

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.ITERATORS_N_GENERATORS;


/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */
/* eslint-disable yield-star-spacing */
const bad = () => {
  function* generator () {
    yield * other();
  }
  function* generator () {
    yield *other();
  }
  function* generator () {
    yield*other();
  }
};
/* eslint-enable yield-star-spacing */

const good = () => {
  function* generator () {
    yield* other();
  }
};

const best = null;
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

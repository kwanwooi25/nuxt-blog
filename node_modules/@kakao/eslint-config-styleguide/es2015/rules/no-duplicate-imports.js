const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '모듈 당 하나의 import 구문을 사용한다.';

const rule = 'no-duplicate-imports';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.MODULES;

const bad = `
import {merge} from 'module';
import something from 'another-module';
import {find} from 'module';
`;

const good = `
import {merge, find} from 'module';
import something from 'another-module';
`;

const best = null;

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

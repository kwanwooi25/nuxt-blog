const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = `객체 리터럴 속성에 싱글 쿼테이션(')이나 더블 쿼테이션(")을 사용하지 않는다.`;

const rule = 'quote-props';

const desc = '단, 키에 대쉬(-)나 공백 등이 포함될 경우에는 싱글 쿼테이션을 사용한다.';

const options = [
  'error',
  'as-needed',
  {
    keywords: false,
    unnecessary: true,
    numbers: false
  }
];

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.OBJECTS;

/* eslint-disable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, quotes */
/* eslint-disable quote-props */
const bad = () => {
  const bad = {
    'foo': 3,
    "bar": 4,
    "data-blah": 5
  };
};
/* eslint-enable quote-props */

const good = () => {
  const good = {
    foo: 3,
    bar: 4,
    'data-blah': 5,
    'my-name': 5
  };
};

const best = null;
/* eslint-enable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, quotes */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best
};

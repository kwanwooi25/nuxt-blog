const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = '사용되지 않는 플래그는 쓰지 않는다.';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.VARIABLES;

const airbnbCategory = AIRBNB_CATEGORY.VARIABLES;

const disableRule = {
  name: 'no-unused-vars',
  option: 'off',
};

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  disableRule
};

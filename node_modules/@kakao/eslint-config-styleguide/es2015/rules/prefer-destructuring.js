const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '객체 또는 배열에서 복수의 프로퍼티에 접근할 때는 비구조화를 사용한다.';

const rule = 'prefer-destructuring';

const desc = null;

const options = ['error', {object: true,
  array: true}];

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.PROPERTIES;

/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */
/* eslint-disable prefer-destructuring */
const bad = () => {
  function getCrewName (crew) {
    const nickName = crew.nickName;
    const realName = crew.realName;

    return `${nickName} ${realName}`;
  }

  const foo = array[0];
  const bar = obj.bar;
};
/* eslint-enable prefer-destructuring */

const good = () => {
  function getCrewName ({nickName, realName}) {
    return `${nickName} ${realName}`;
  }

  const [foo] = array;
  const {bar} = obj;
};
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
};

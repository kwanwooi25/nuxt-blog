const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = '클래스 생성자에서 인자 프로퍼티를 사용하지 않는다.';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.CLASSES_N_CONSTRUCTORS;

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory
};

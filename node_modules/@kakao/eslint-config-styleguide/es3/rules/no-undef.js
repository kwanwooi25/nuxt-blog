const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '네임스페이스를 사용하여 전역변수를 최소화한다.';

const rule = 'no-undef';

const desc = `암묵적인 전역변수는 사용하지 않는다.
불가피하게 전역변수를 사용해야 한다면  /* global ...*/ 주석을 파일 상단에 기재한다.`;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.VARIABLES;

const airbnbCategory = AIRBNB_CATEGORY.VARIABLES;

/* eslint-disable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable */
/* eslint-disable no-undef */
const bad = () => {
  var a = someFunction();
  b = 10;
};
/* eslint-enable no-undef */

const good = () => {
  /* global someFunction b:true */

  var a = someFunction();
  b = 10;
};

const best = null;
/* eslint-enable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best
};

switch (a) {
  case 'a':
    break;
  case 'b':
    break;
}

(function () {
  function foo (x) {
    return x + 1;
  }
})();

foo
  .bar
  .baz();

foo(bar,
  baz,
  qux
);

const foo = [
  bar,
  baz,
  qux
];

if (foo) {
  doSomething();
  doSomethingElse();
}

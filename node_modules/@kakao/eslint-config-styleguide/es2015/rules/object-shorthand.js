const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '메서드 문법을 사용한다.';

const rule = 'object-shorthand';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.CLASSES_N_CONSTRUCTORS;


/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare, object-shorthand */
const bad = () => {
  const foo = {
    w: function () {},
    x: function* () {},
    [y]: function () {},
    z: z,
  };
};

const good = () => {
  const foo = {
    x: y => y,
  };
};

const best = () => {
  const foo = {
    w () {},
    * x () {},
    [y] () {},
    z,
  };
};
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare, object-shorthand */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

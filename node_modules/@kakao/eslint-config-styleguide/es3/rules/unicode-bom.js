const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '유니코드 BOM(Byte Order Mark)을 사용하지 않는다.';

const rule = 'unicode-bom';

const desc = '유니코드 BOM(Byte Order Mark)은 코드 단위가 빅 endian인지 또는 little endian인지 지정하는 데 사용됩니다.';

const options = 'error';

const cause = 'UTF-8은 BOM 을 필요로하지 않습니다.';

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.STRINGS;

/* eslint-disable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, space-infix-ops */
/* eslint-disable unicode-bom */
const bad = () => {
  U+FEFF;
  var abc;
};
/* eslint-enable unicode-bom */

const good = null;

const best = null;
/* eslint-enable no-undef, no-unused-vars, no-redeclare, no-magic-numbers, no-unused-expressions, no-self-compare, no-cond-assign, default-case, no-unreachable, space-infix-ops */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best
};

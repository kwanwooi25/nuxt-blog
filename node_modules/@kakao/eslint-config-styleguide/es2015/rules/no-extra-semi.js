const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../lib/generator/constants');

const title = '메서드, 클래스 리터럴의 닫는 중괄호 뒤에 세미콜론을 생략한다.';

const rule = 'no-extra-semi';

const desc = null;

const options = 'error';

const cause = null;

const lintCategory = LINT_CATEGORY.STYLISTIC_ISSUES;

const airbnbCategory = AIRBNB_CATEGORY.SEMICOLONS;

/* eslint-disable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */
/* eslint-disable no-extra-semi */
const bad = () => {
  class Foo {
    constructor () {
      this.x = 42;
    }
    method () {
      return this.x;
    };
  };
};
/* eslint-enable no-extra-semi */

const good = () => {
  class Foo {
    constructor () {
      this.x = 42;
    }
    method () {
      return this.x;
    }
  }
};

const best = null;
/* eslint-enable no-unused-vars, no-undef, no-unused-expressions, no-redeclare */

module.exports = {
  title,
  rule,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  bad,
  good,
  best,
};

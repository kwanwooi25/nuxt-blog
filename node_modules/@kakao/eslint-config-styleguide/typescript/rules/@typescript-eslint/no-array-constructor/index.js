const {LINT_CATEGORY, AIRBNB_CATEGORY} = require('../../../../lib/generator/constants');

const title = '예외 경우를 제외하고 제네릭 배열 생성자를 사용하지 않는다.';

const desc = '단, 특정 크기를 가진 sparse array를 생성하거나, 배열의 타입 선언을 위해 사용하는 경우는 허용된다.';

const options = 'error';

const cause = '제네릭 배열 선언 시 단일 인수의 잘못된 사용, 배열 전역을 다시 정의하는 문제가 있을 수 있기 때문에 리터럴 생성자를 사용하는 것을 지향한다.';

const lintCategory = LINT_CATEGORY.BEST_PRACTICES;

const airbnbCategory = AIRBNB_CATEGORY.ARRAYS;

const disableRule = {
  name: 'no-array-constructor',
  option: 'error',
};

module.exports = {
  title,
  options,
  desc,
  cause,
  lintCategory,
  airbnbCategory,
  disableRule
};
